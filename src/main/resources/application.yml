spring:
  application:
    name: smart-farmer-ai
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/smart_farmer_ai}
    username: ${DATABASE_USERNAME:smartfarmer}
    password: ${DATABASE_PASSWORD:smartfarmer}
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
        format_sql: false
        default_batch_fetch_size: 50
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
server:
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: /

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never
  info:
    env:
      enabled: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha
  api-docs:
    path: /v3/api-docs

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000}
  jwt:
    # No production default on purpose: startup fails unless JWT_SECRET holds >= 32 characters.
    secret: ${JWT_SECRET:}
    access-expiration: ${JWT_ACCESS_EXPIRATION:900}
    refresh-expiration: ${JWT_REFRESH_EXPIRATION:2592000}
  # Leave base-url empty to keep the AI boundary disabled: requests are then stored as PENDING
  # instead of being answered with fabricated predictions.
  ai-service:
    base-url: ${AI_SERVICE_BASE_URL:}
    api-key: ${AI_SERVICE_API_KEY:}
    timeout: ${AI_SERVICE_TIMEOUT:5000}
  gemini:
    api-key: ${GEMINI_API_KEY:}
    model: ${GEMINI_MODEL:gemini-2.5-flash}
  # Without a weather provider the weather endpoints answer 503 rather than placeholder data.
  weather:
    base-url: ${WEATHER_API_BASE_URL:}
    api-key: ${WEATHER_API_KEY:}
    timeout: ${WEATHER_API_TIMEOUT:5000}
  storage:
    local-directory: ${FILE_STORAGE_PATH:uploads}
    max-file-size-bytes: ${FILE_STORAGE_MAX_SIZE_BYTES:10485760}
    allowed-content-types:
      - image/jpeg
      - image/png
      - image/webp
