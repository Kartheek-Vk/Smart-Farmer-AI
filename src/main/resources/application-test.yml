spring:
  jpa:
    show-sql: false
  flyway:
    clean-disabled: false

logging:
  level:
    root: WARN
    com.smartfarmer.ai: INFO

app:
  jwt:
    secret: ${JWT_SECRET:local-development-secret-local-development-secret}
  otp:
    expose-dev-code-in-response: true
